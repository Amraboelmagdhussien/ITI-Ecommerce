# ITI-Ecommerce

Font used Inter

      async function test() {
        await addToCart("0");
        await addToCart("1");
        await addToCart("2");

        console.log(cartItems);
      }

      // addToCart("0").then((value) => {
      //   console.log(value.val());
      // });
